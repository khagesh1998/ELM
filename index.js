/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import {Bootstrap} from './action-land/bootstrap';

AppRegistry.registerComponent(appName, () => Bootstrap);
